version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: posify-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      # This is the connection string for the mongo service defined below.
      - MONGODB_URI=mongodb://mongo:27017/posapp-marketing
      - JWT_SECRET=your-production-jwt-secret
      # Demo data is disabled for this Docker setup.
      # Change to 'true' if you want to load demo data on first run.
      - LOAD_DEMO_DATA=false
      # Add your Gemini API key here to enable AI features.
      - GEMINI_API_KEY=
    depends_on:
      - mongo
    networks:
      - posify-net
    restart: unless-stopped

  mongo:
    image: mongo:latest
    container_name: posify-mongo
    ports:
      - "27017:27017"
    volumes:
      - marketing-mongo-data:/data/db
    networks:
      - posify-net
    restart: unless-stopped

networks:
  posify-net:
    driver: bridge

volumes:
  marketing-mongo-data:
    driver: local
